# Security

Helm is a local-only macOS terminal emulator. It does not make outbound network connections except:
- AI harness API calls (made by the harness you launch, not Helm itself)
- kaku-relay (disabled by default, requires explicit opt-in)

To report a security issue, email: security@doabit.dev
